# Chapter 11 - Arrays

## Files

| File | Description |
|------|-------------|
| `83_Arrays.js` | Array creation with literals, access by index, basic iteration |
| `84_Arrays.js` | Array constructor, `Array.of()`, `Array.from()` |
| `85_Access_Array.js` | Accessing elements with `[]`, `at()`, and modifying values |
| `86_Arrays_Adding_Remove.js` | `push`, `pop`, `unshift`, `shift` — adding/removing elements |
| `87_Adding_Remove2.js` | `splice` — insert, remove, and replace elements |
| `88_REAL_Example.js` | Real-world: browser list manipulation with shift and loop |
| `89_Searching.js` | `indexOf`, `lastIndexOf`, `includes`, `find`, `findIndex`, `findLast`, `findLastIndex` |
| `90_iterate.js` | Iteration methods: `for`, `for...of`, `forEach`, `for...in`, `entries()` |
| `91_Transform_Array.js` | `map`, `filter`, `reduce`, `flat` — transforming arrays |

## Run

```bash
node <filename>.js
```
