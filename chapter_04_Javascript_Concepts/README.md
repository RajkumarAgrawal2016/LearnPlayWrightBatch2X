# Chapter 04 - JavaScript Concepts (var, let, const, Hoisting)

## Files

| File | Description |
|------|-------------|
| `09_car_let_cont.js` | `var` vs `let` vs `const` — redeclaration and reassignment |
| `10_functions.js` | Function declaration and invocation |
| `11_var_explained.js` | `var` — global vs function scope |
| `12_let_people_love.js` | `let` — block scoped variable |
| `13_const_explained.js` | `const` — constant declaration and reassignment error |
| `14_var_functionscope.js` | `var` function scoping behavior |
| `15_let_scope.js` | `let` block scoping vs function scope |
| `16_Hoisting.js` | Hoisting with `var` — `undefined` vs reference |
| `17_hoisting_fn.js` | Hoisting inside functions |
| `18_let_hoisting.js` | `let` hoisting and Temporal Dead Zone (TDZ) |
| `19_let_hoisting_block.js` | TDZ behavior in block scope with `let` |
| `20_let_const.js` | `const` hoisting and TDZ (reference error) |
| `21_Jr_QA.js` | TDZ with `const` — accessing before initialization |

## Run

```bash
node <filename>.js
```
